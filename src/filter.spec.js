const filter = require('./filter');

describe('Filter', () => {

    it('should return list when empty list is passed', () => {
        const fun = () => true;

        expect(filter([], fun)).toEqual([]);
    })

    it('', () => {
        
    })

    it('', () => {
        
    })

    it('', () => {
        
    })

    it('', () => {
        
    })

})